package com.example.model.entities;

class GroupTest {
    /*
    static Group group;
    static Student student1;
    static Student student2;
    static Student student3;

    @BeforeAll
    static void init() {
        group = new Group("Test1");
        student1 = new Student("student1fn", "student1mn", "studen1sn", LocalDate.now(), "+71234567890");
        student2 = new Student("student2fn", "student2mn", "studen2sn", LocalDate.now(), "+71234567890");
        student3 = new Student("student3fn", "student3mn", "studen3sn", LocalDate.now(), "+71234567890");
        group.addStudent(student1);
    }

    @Test
    void test_hasNumber() {
        assertTrue(group.hasNumber("Test1"));
        assertFalse(group.hasNumber("otherString"));
    }

    @Test
    void test_hasStudent_by_obj() {
        assertTrue(group.hasStudent(student1));
        Student student2 = new Student("student2fn", "student2mn", "studen2sn", LocalDate.now(), "+71234567890");
        assertFalse(group.hasStudent(student2));
    }

    @Test
    void test_hasStudent_by_id() {
        assertTrue(group.hasStudent(student1.getId()));
        assertFalse(group.hasStudent(student2.getId()));
    }

    @Test
    void addStudent() {
        assertTrue(group.addStudent(student2));
        assertFalse(group.addStudent(student2));
    }

    @Test
    void getStudentsQuantity() {
        assertEquals(2, group.getStudentsQuantity());
    }
    @Test
    void  equalsAndHashCode() {
        Group group1 = new Group("number");
        Group group2 = new Group("number");
        assertNotEquals(group1, group2);
        assertNotEquals(group1.hashCode(), group2.hashCode());
    }

     */
}